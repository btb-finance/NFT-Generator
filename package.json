{
  "name": "pixel-cats-nft-generator",
  "version": "1.0.0",
  "description": "Generate unique pixel art cat NFTs",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "generate": "bun run src/generate.ts",
    "onchain": "bun run src/generateOnChain.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun"
  },
  "keywords": ["nft", "pixel-art", "generator", "cats"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
